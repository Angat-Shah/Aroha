/**
 * Index component - Legacy page, not currently used
 * The Welcome.tsx component is now used as the landing page
 */

const Index = () => {
  // This page is not used in current routing
  return null;
};

export default Index;
